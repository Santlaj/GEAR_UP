services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: lmcs
      POSTGRES_PASSWORD: lmcs
      POSTGRES_DB: lmcs
    ports:
      - "5433:5432"
    volumes:
      - ./backend/sql/rls.sql:/docker-entrypoint-initdb.d/01-rls.sql:ro
